// Reserved for a shared status badge implementation. Keeping this component
// isolated gives detail views and table rows one place to share presentation
// when the current inline badges are consolidated.
export default function StatusBadge() {
  return <div>Status Badge</div>;
}
